import { Text, View } from "react-native";

export function NativeWindSmokeTest() {
  return (
    <View className="rounded-lg border border-border bg-surface px-4 py-3">
      <Text className="text-sm font-semibold text-primary">NativeWind ready</Text>
      <Text className="mt-1 text-xs text-text-secondary">FinderZ tokens are available.</Text>
    </View>
  );
}
